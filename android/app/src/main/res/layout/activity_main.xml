<?xml version="1.0" encoding="utf-8"?>
<!-- Native landing screen for Android. Edit this when the phone app grows beyond opening the web dashboard. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/activity_background"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="28dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-condensed"
        android:gravity="center"
        android:letterSpacing="0.16"
        android:text="@string/app_name"
        android:textAllCaps="true"
        android:textColor="@color/dropzone_text_primary"
        android:textSize="28sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:gravity="center"
        android:lineSpacingExtra="4dp"
        android:text="@string/android_app_intro"
        android:textColor="@color/dropzone_text_secondary"
        android:textSize="15sp" />

    <Button
        android:id="@+id/open_dashboard_button"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginTop="28dp"
        android:background="@drawable/button_primary_background"
        android:fontFamily="sans-serif-medium"
        android:text="@string/open_live_dashboard"
        android:textAllCaps="false"
        android:textColor="@color/dropzone_text_primary"
        android:textSize="15sp" />

</LinearLayout>
