<?xml version="1.0" encoding="utf-8"?>
<!-- Native launcher container for a bitmap-rendered Rank Pulse card. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rank_pulse_widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/rank_pulse_widget_bitmap"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/widget_description"
        android:scaleType="fitXY" />

    <!-- Invisible tap target over the dashboard-style sync pill. -->
    <TextView
        android:id="@+id/widget_open_app_button"
        android:layout_width="96dp"
        android:layout_height="44dp"
        android:layout_gravity="top|end"
        android:background="@android:color/transparent"
        android:contentDescription="@string/widget_open_description" />
</FrameLayout>
