# Temporary workflow used to confirm GitHub Actions can start jobs for this repository.
name: Actions Smoke Test

on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm runner starts
        run: echo "GitHub Actions runner started."
